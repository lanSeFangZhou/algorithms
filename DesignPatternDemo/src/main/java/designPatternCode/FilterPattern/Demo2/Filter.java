package designPatternCode.FilterPattern.Demo2;

//import com.sun.tools.javac.util.StringUtils;

//import java.util.stream.Collectors;

public class Filter {
//    Map<Integer, List<Person>> groupMap = persons.stream().collect(
//            Collectors.groupingBy(Person::getGender))
//    groupMap.forEach((k, v) -> {
//        System.out.println(k);
//        v.foreach(System.out.println(System.out::println));
//    })

//    List<String> aList = Lists.newArrayList("1", "2", "");
//    aList.stream().filter(x->StringUtils.isNotEmpty(x));
}
